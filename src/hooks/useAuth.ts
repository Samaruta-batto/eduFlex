// src/hooks/useAuth.ts
export const useAuth = () => {
  // In a real application, you would get the user's role from your authentication system.
  // For this example, we'll just return a mock role.
  return { role: 'admin' };
};
